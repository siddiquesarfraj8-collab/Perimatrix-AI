This commit corrects the earlier commit message that incorrectly said "backend".

The frontend files were initialized in the earlier commit; this commit's message clarifies that intent:

"feat: initialize frontend architecture"

File created to record this non-destructive correction.